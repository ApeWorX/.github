name: PR Title

on:
  workflow_call:

# NOTE: Use as follows:
#  on:
#    pull_request_target:
#      types:
#      - opened
#      - edited
#      - synchronize
#
#  jobs:
#    check-title:
#      uses: ApeWorX/.github/.github/workflows/prtitle.yaml@main

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
    - uses: astral-sh/setup-uv@v7
    - name: Check PR Title
      run: uvx commitlint "${{ github.event.pull_request.title }}"
